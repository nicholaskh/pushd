language: go

go:
  - 1.4

before_install:
  - cp .netrc ~
  - chmod 600 .netrc

install:
  - go get -u github.com/nicholaskh/pushd/daemon

script:
  - go test ./... -v -race

notifications:
  email: false
